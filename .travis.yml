language: ruby
rvm:
  - 1.9.2
env:
  - DB=mysql GOVUK_APP_DOMAIN=test.gov.uk RUBYOPT="-rpsych"
services:
  - elasticsearch
script:
  - bundle exec rake
branches:
  except:
    - release
notifications:
  email: false
