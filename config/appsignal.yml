default: &defaults
  push_api_key: "<%= ENV['APPSIGNAL_API_KEY'] %>"
  name: "Rummager"
  active: <%= ENV['APPSIGNAL_API_KEY'] ? true : false %>

development:
  <<: *defaults

production:
  <<: *defaults
